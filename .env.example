# AMP API settings
AMP_BASE_URL=http://127.0.0.1:8080
AMP_API_TOKEN=replace_with_amp_api_token
# Common AMP API path for listing instances. Change this if your AMP install uses a different route.
AMP_INSTANCE_LIST_ENDPOINT=/API/ADSModule/GetInstances
# Webhook-driven mode can still keep an occasional full sync as safety (seconds).
# Set to 0 to disable periodic fallback.
PERIODIC_SYNC_SECONDS=300

# Webhook listener settings
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8787
WEBHOOK_PATH=/amp-webhook
# Optional shared secret. If set, sender must provide it in X-Webhook-Token
# or Authorization: Bearer <token>.
WEBHOOK_TOKEN=

# Cloudflare settings
CLOUDFLARE_API_TOKEN=replace_with_cloudflare_api_token
CLOUDFLARE_ZONE_ID=replace_with_zone_id_for_cobyas_xyz

# DNS constraints
ALLOWED_DOMAIN=cobyas.xyz
# SRV service and protocol portions: _service._proto.<name>
SRV_SERVICE=_minecraft
SRV_PROTO=_tcp
SRV_PRIORITY=0
SRV_WEIGHT=0
SRV_TTL=60

# Fallback target hostname if AMP instance data does not include one.
# Usually your game node's public DNS name, for example: node1.cobyas.xyz
DEFAULT_TARGET=

# Optional: comma-separated list of instance names to ignore
IGNORE_INSTANCE_NAMES=
